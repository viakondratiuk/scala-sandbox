package HRMS

object My_Monad_Attempt extends App {
//
//  trait Attempt[A] {
//    def flatMap[B](f: A => Attempt[B]): Attempt[B]
//  }
//  object Attempt {
//    def apply[A](a): Attempt[A] = new Attempt[] {}
//  }
//
//  trait M[T] {
//    def map[S](f: T => S): M[S]
//    def flatMap[S](f: T => M[S]): M[S]
//  }
}

