package HRMS

object Map_flatMap_Filter extends App {
  /**
  Bloom Taxonomy
	- Remember, Understand, Apply, Analyse, Evaluate, Create

	- Apply is interpretation, sketching where the concept fits
	- Analyze is comparing and contrasting other concepts and facts to new information
	- Evaluate is discriminating which connections between concepts are the most important.
   */

  /** Map, FlatMap, Filter **/

  /**
  A. Hierarchy and Related Concepts
    HOF - takes functions or returns functions
    Transform collections in functional way
    - map - transforms collection to another one
    - filter - removes elements by condition
    - flatMap - map and flatten, transform and remove 1 level of nested
   */

  /**
  B. What problem solves? Abstract and Real
    Iterate collections in functional way

   */

  /**
  C. Code Example
   */
//  def map[B](f: A => B): List[B]
//  def filter(f: A => Boolean): List[A]
//  def flatMap[B](f: A => List[B]): List[B]
}
